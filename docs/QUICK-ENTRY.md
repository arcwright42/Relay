# 全局快捷入口与网页上下文

需求确认：2026-09-24。本文是首版实施规格；各项实现与验证状态以文末为准。

## 首版交互

Relay 常驻 macOS，关闭工作台不退出；用户通过全局快捷键选中文字并唤起小窗。首批验收 Chrome、Edge、Safari 的普通网页。选区不可用时允许粘贴，不复用上一次选区。自动划词浮现、右键菜单接管、浏览器扩展、截图/OCR 暂缓。

动作包含 AI 联网搜索、解释、翻译、摘要、加入项目，以及自由输入。AI 搜索由 Agent 实际调用联网能力后综合回答、引用来源，不能把模型已有知识包装成搜索结果。“加入项目”只保存材料，不触发 Agent。

每次唤起在面板激活前捕获选中文字和可取得的来源 URL。用户明确选择目标项目；回复、追问和展开工作台继续该项目主对话。首版不引入额外任务线程。项目已有运行轮次时提示等待，不覆盖工作台草稿、不重复发送。关闭小窗不取消运行，停止按钮才取消任务。

## Relay 负责 web fetch

Moli 的 `fetch` 由 Relay 调用，用于补充当前选区的网页背景，与 Agent 的后续联网搜索独立。

1. 捕获选区，尽力取得 HTTP(S) URL；立即展示面板。
2. 有 URL 时后台预取网页正文，与用户选择动作同时进行。没有 URL 时跳过。
3. 用户点击动作或发送问题时，立即冻结本轮输入：动作/问题、选中文字、来源 URL、已成功获取的正文。
4. 抓取仍在进行、失败、超时或 Moli 不可用时，直接发送选区和已有来源，绝不等待抓取。
5. 发送之后迟到的正文不追加到本轮、不自动重发；下一次捕获不接收上一次抓取的结果。

URL 采集属于桌面平台层，Moli 根据 URL 重新加载网页，不能代替读取用户当前标签页选区，也不默认拥有用户浏览器的登录状态。URL 缺失不影响 AI 搜索。

正文有长度上限，截取必须显式标记。外部材料以引用形式与用户指令分开，保留来源；网页内容不成为项目指令或权限依据。读取失败不默认为空白成功。不自动导入浏览器 Cookie。

## 实现边界

- 平台层：全局快捷键注册、辅助功能选区读取、来源地址采集；按需引导辅助功能权限。
- `relay-core`：采集快照、动作及 prompt 组装，网页抓取服务接口。
- `relay-runtime`：Moli 子进程、超时和输出限制，后台抓取结果。
- `relay-ui`：项目选择、上下文预览、动作、小窗流式回复和展开。
- `relay`：服务装配、常驻与窗口生命周期。

Moli 优先以独立 CLI 接入，不把浏览器内核编入 Relay。AI 联网搜索由当前 Agent 的搜索能力承担；后续可另接 moli-websearch。仅安装 fetch 不代表 Agent 已具备搜索能力。

## 验收

- Chrome、Edge、Safari 普通网页：先捕获选区再激活小窗；失败显示粘贴入口。
- 无 URL、抓取失败、超时、抓取未完成都不阻塞发送。
- 连续唤起、切换项目、发送后抓取完成不会串用材料或触发重复请求。
- 小窗与工作台共享项目历史和运行状态，工作台草稿不被覆盖。
- 全局快捷键冲突明确提示；关闭最后一个窗口仍可唤起；明确退出释放注册与后台进程。
- AI 搜索需要真实网络与 Agent 联调验证来源，不以提示词存在证明搜索成功。

## 实现记录

- 默认快捷键为 `Control + Option + Space`，使用系统注册和事件通道，空闲时不轮询操作系统输入。关闭最后一个窗口保持常驻；Dock 或 Relay 菜单重开工作台，菜单可打开手动输入面板或退出。
- 小窗选择目标项目，复用项目主对话、流式事件、权限请求和取消。工作台与小窗保留各自草稿；展开时最小化小窗，保留未发送输入。首版尚未接入快捷入口的 Jev 自动归属。
- macOS 平台层在浮窗激活前读取 `AXSelectedText`、应用名称和窗口 `AXDocument`。地址不暴露时不抓取；没有选区时可粘贴。首次需手动授予辅助功能权限。尚未宣称 Chrome、Edge、Safari 全部通过实机验收。
- Moli 固定 `1.1.10`，首次有 URL 时在后台从官方 GitHub Release 下载当前架构组件，校验 SHA-256 和版本后原子激活；许可证随组件保留。安装也不阻塞发送。`RELAY_MOLI_PATH` 可指定本地版本。
- 正常页面抓取参数为 8 秒，Relay 在 9 秒时终止未结束的进程；最多两个并行抓取，输出上限 256 KiB，正文最多 12,000 字并标记截取。退出时取消组件准备及抓取。不读取浏览器 Cookie，不自动抓取内网地址。
- AI 搜索将明确的联网检索及引用要求交给当前项目 Agent；实际可用工具由该 Agent 的配置决定。没有执行真实检索时应明确说明，不能宣称搜索成功。本次不修改用户已有 Agent 搜索配置。
- 新增自动化覆盖抓取未完成/失败时发送、迟到正文、连续唤起、工作台草稿隔离、项目忙碌、加入资料不发 Agent，以及子进程失败、输出上限和超时。真实公开网页抓取通过独立 probe 验证；真实浏览器权限、多屏与 Agent 联网搜索结果仍需交互验收。
- 本次 `cargo xtask verify` 通过包边界、格式、全目标 Clippy 与 68 个测试；`cargo xtask bundle` 生成 `dist/Relay.app`。隔离数据目录启动检查通过，没有读取个人项目或发起模型请求。Moli 1.1.10 通过实际 runtime probe 读取 `https://example.com`，返回 167 字符正文；未宣称已完成真实 Agent 联网搜索联调。

## 紧凑浮层交互（2026-09-24）

- 快捷键先打开 640 × 54 的横向工具条：AI 搜索、解释、翻译、更多、问问 Relay。工具条使用非激活浮窗与 macOS 模糊背景，不再展示工作台式大输入框或历史对话。按鼠标所在屏幕定位，并为结果面板预留屏幕边界空间。
- 更多中选择项目、加入资料和处理权限；首次未授权时工具条保留权限图标，点击申请系统辅助功能权限。用户授权后重新触发采集，不复用旧选区。
- 点击动作展开对应面板：解释 480 × 360、翻译 480 × 460、AI 搜索 520 × 580。显示实际选中文字和本次回复，隐藏内部组装提示词及之前的项目消息。结果可复制、停止、追问或在原项目工作台打开。
- 翻译面板显示自动检测与目标语言选择；选择新的语言会重新发送翻译请求。问问 Relay 支持 Enter 发送、Shift+Enter 换行。网页抓取仍不阻塞发送。
- 布局使用 GPUI 离屏渲染检查；自动化补充工具条按钮可见性、旧回复隔离、复制本次回复和翻译语言请求。离屏回答为测试夹具，不作为真实 Agent 联网检索的验收依据。

本轮 `cargo xtask verify` 通过包边界、格式、Clippy 和 70 个测试。

## 空选区误答修复

发现实际请求未携带选区和 URL 时，Agent 会把“这是什么项目”误解为 Relay 项目。平台采集增加焦点祖先、网页区域与 WebKit 文本标记范围读取，兼容 CFURL 地址；遍历设置时间、深度和节点上限。未读取选区时工具条和结果面板明确提示，辅助功能授权使用可见文字入口。请求明确区分当前选区与存档项目；没有材料时要求澄清，不以项目信息替代。自动化 71 项通过；浏览器授权后实机读取仍需用户重试验证。
